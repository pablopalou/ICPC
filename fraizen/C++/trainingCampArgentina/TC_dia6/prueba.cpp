#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
 #include <cmath>
#define ll long long
#define pb push_back
#define fr(a,b) for(int i = a; i<b; i++)
#define rep(i,a,b) for(int i = a; i<b; i++)
#define mod 1000000007
#define inf (1LL<<60)
#define all(x) (x).begin(),(x).end()
#define prDouble(x) cout << fixed << setprecision(10) << x
#define triplet pair<ll,pair<ll,ll>>
#define goog(tno) cout << "Case #" << tno << ": " 
#define fast_io ios_base::sync_with_stdio(false);cin.tie(NULL)
#define read(x) int x; cin >> x
#define cout(x) cout << x << '\n'

typedef pair<int, int> pii;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // int n,t1,t2;
    // cin >> n >> t1 >> t2;
    // vector<int> cap(n); 
    // fr(0,n){
    //     cin >> cap[i];
    // }
    int a = 4;
    int b = ceil(2.1);
    cout(b);
    // vector<pii> arrWithIndex(n);

    // for (int i = 0; i < n; i++) {
    //     arrWithIndex[i] = make_pair(cap[i], i);
    // }

    // sort(arrWithIndex.begin(), arrWithIndex.end(),
    //      [](const pii& a, const pii& b) { return a.first < b.first; });

    // bool a, b;
    // a = solve(t1,t2,arrWithIndex);
    // if (!a){
    //     b = solve(t2,t1,arrWithIndex);
    // }
    // if (!b){
    //     cout("No");
    // }
}